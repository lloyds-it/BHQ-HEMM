void downloadFile(String url, String filename) {
  // Mobile / Desktop stub: print URL since direct web anchor clicks don't apply.
  // In a future feature, this can be integrated with flutter_downloader or path_provider.
  print('Downloader stub: Triggered download for $filename from $url');
}
